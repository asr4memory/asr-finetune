{
  "_comment": "Search-space description for the Phase-1 hail-mary HPO run. Used by create_optuna_db.py --space when warm-starting the new Optuna study from the prior Ray-Tune trials, so old trials are accepted under the new (narrower + lora_dropout-extended) distributions.",
  "config.train_loop_config.per_device_train_batch_size": {
    "type": "categorical",
    "choices": [8]
  },
  "config.train_loop_config.learning_rate": {
    "type": "float",
    "low": 1e-6,
    "high": 3e-5,
    "log": true
  },
  "config.train_loop_config.warmup_ratio": {
    "type": "categorical",
    "choices": [0.03, 0.05, 0.1, 0.15]
  },
  "config.train_loop_config.alpha": {
    "type": "categorical",
    "choices": [8, 16, 32]
  },
  "config.train_loop_config.target_r": {
    "type": "categorical",
    "choices": [4, 8, 12, 16]
  },
  "config.train_loop_config.lora_dropout": {
    "type": "categorical",
    "choices": [0.0, 0.05, 0.1]
  }
}
