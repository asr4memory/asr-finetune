# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# OS / editor / tooling
.DS_Store
.idea/
.vscode/
.claude/

# Local data, models, and run artifacts — never commit these
/data
/data_example
/models_local
output/
ray_results/
*.db
*.h5
*.parquet
slurm-*.out
*.log

# The paper manuscript is distributed via the published article, not the repo
docs/paper/*.docx

# Legacy directories left on disk after the modernization (kept locally, never tracked)
/finetune/
/sessions/
/asr-classification/

# Tooling / assistant session logs
.claude-max/
