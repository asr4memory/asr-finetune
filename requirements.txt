# Core dependencies for Whisper LoRA/DoRA fine-tuning + Ray Tune HPO + WER evaluation.
#
# IMPORTANT — PyTorch: install the wheel matching your CUDA toolkit SEPARATELY first,
# then install the rest of this file. For example (CUDA 12.4):
#     pip install torch==2.5.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu124
#     pip install -r requirements.txt
# Use cu121 / cu124 etc. to match your cluster's CUDA. The pins below let pip
# treat an already-installed matching torch as satisfied.

torch==2.5.1
torchaudio==2.5.1

transformers==4.46.3
peft==0.14.0            # >=0.9 required for DoRA (use_dora=True) + PiSSA init
accelerate==1.1.1
datasets==3.1.0
evaluate==0.4.3
jiwer==3.0.5

ray[tune,train,data,default]==2.40.0
optuna==4.1.0

deepspeed==0.16.5       # ZeRO-3 / CPU-offload; only needed for large-model or multi-GPU runs

configargparse==1.7
h5py==3.13.0
soundfile==0.10.3.post1
librosa==0.9.2
numpy
pandas
pyarrow
safetensors
psutil
tqdm
sympy==1.13.1
tensorboard==2.18.0
tensorboardX==2.6.2.2

# Optional extras (install on demand, not required for the core pipeline):
#   speaker diarization  (src/evaluation/transcribe_diarize.py):  pip install "pyannote.audio"
